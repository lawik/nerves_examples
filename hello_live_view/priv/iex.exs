# What the IEx window evaluates first on the host, where the firmware's
# /etc/iex.exs (rootfs_overlay/etc/iex.exs) does not exist: the same helpers
# the SSH console gets, without the MOTD.
use Toolshed
